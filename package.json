{
  "name": "envbeam",
  "version": "0.11.2",
  "description": "Beam your whole dev environment to any machine or VM. Pause here, resume there: code + secrets + container + database + AI session, under the right account for each.",
  "type": "module",
  "bin": {
    "envbeam": "dist/cli.js"
  },
  "exports": {
    ".": "./dist/index.js",
    "./schema": "./dist/core/config/schema.js"
  },
  "files": [
    "dist",
    "schema/envbeam.schema.json",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepare": "npm run build",
    "dev": "tsx src/cli.ts",
    "envbeam": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:unit": "vitest run test/unit",
    "test:integration": "vitest run test/integration",
    "coverage": "vitest run --coverage",
    "schema:gen": "tsx scripts/gen-schema.ts",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build && npm run schema:gen"
  },
  "keywords": [
    "cli",
    "devcontainer",
    "secrets",
    "doppler",
    "1password",
    "postgres",
    "claude-code",
    "dev-environment",
    "sync",
    "git"
  ],
  "author": "Alex Gvozden",
  "license": "MIT",
  "dependencies": {
    "@inquirer/prompts": "^7.2.1",
    "commander": "^13.0.0",
    "picocolors": "^1.1.1",
    "yaml": "^2.6.1",
    "zod": "^3.24.1",
    "zod-to-json-schema": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@vitest/coverage-v8": "^3.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
